# Зависимости, необходимые ТОЛЬКО для работы сервиса в контейнере
# Отдельный файл нужен, чтобы не тащить в production-образ pytest и kagglehub
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.2.6
joblib==1.5.3
Flask==3.1.3
gunicorn==23.0.0
